import { VietNameseDatePipe } from './vietnamese-date.pipe';

describe('VietNameseDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new VietNameseDatePipe();
    expect(pipe).toBeTruthy();
  });
});
