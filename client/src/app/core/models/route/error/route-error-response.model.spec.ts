import { RouteErrorResponse } from './route-error-response.model';

describe('RouteErrorResponse', () => {
  it('should create an instance', () => {
    expect(new RouteErrorResponse()).toBeTruthy();
  });
});
